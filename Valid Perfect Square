class Solution {
    public boolean isPerfectSquare(int num) {
        int c=0;
        for(int i=1;i<=num/i;i++){
            if(num==i*i){
                return true;
            }
        }
            return false;
    }
}
