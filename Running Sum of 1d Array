class Solution {
    int sum=0;
    public int[] runningSum(int[] nums) {
        int[] runningsum=new int[nums.length];
        for(int i=0;i<nums.length;i++){
            sum=sum+nums[i];
           runningsum[i]=sum;
        }
        return runningsum;
    }
}
